<%- include("header") -%>

  <!-- LISTA GENERAL -->
  <div class="box" id="heading">
    <h1>Lista general</h1>
  </div>

  <div class="box">
    <% generalItems.forEach(function(item){ %>

      <form action="/delete" method="post">
        <div class="item">
          <input
            type="checkbox"
            name="checkbox"
            value="<%= item._id %>"
            onChange="this.form.submit()"
          >
          <p><%= item.name %></p>
        </div>

        <input type="hidden" name="listName" value="General">
      </form>

    <% }) %>

    <% if (generalItems.length === 0) { %>
      <div class="item">
        <p>No hay tareas generales. Añade la primera 🙂</p>
      </div>
    <% } %>

    <form class="item" action="/" method="post">
      <input type="text" name="newItem" placeholder="New Item" autocomplete="off">
      <button type="submit" name="list" value="General">+</button>
    </form>
  </div>

  <!-- LISTA TRABAJO -->
  <div class="box" id="heading">
    <h1>Lista de trabajo</h1>
  </div>

  <div class="box">
    <% workItems.forEach(function(item){ %>

      <form action="/delete" method="post">
        <div class="item">
          <input
            type="checkbox"
            name="checkbox"
            value="<%= item._id %>"
            onChange="this.form.submit()"
          >
          <p><%= item.name %></p>
        </div>

        <input type="hidden" name="listName" value="Trabajo">
      </form>

    <% }) %>

    <% if (workItems.length === 0) { %>
      <div class="item">
        <p>No hay tareas de trabajo. Planifica tu día 💼</p>
      </div>
    <% } %>

    <form class="item" action="/" method="post">
      <input type="text" name="newItem" placeholder="New Item" autocomplete="off">
      <button type="submit" name="list" value="Trabajo">+</button>
    </form>
  </div>

<%- include("footer") -%>